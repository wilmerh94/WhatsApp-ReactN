import { Ionicons } from '@expo/vector-icons';

export const TabIcon = ({ size = 24, color = 'black' }) => {
  console.log(
    '🚀 ----------------------------------------------------------🚀',
  );
  console.log('🚀 ~~ file: MainIcon.jsx ~~ line 5 ~~ TabIcon ~~ size');
  console.log(
    '🚀 ----------------------------------------------------------🚀',
  );

  return <Ionicons size={size} color={color} />;
};
